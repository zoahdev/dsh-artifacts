# Everything is a plugin.

DeepSeek Harness is a composable agent harness, and `dsh-artifacts` is how
its output finally *looks* like it belongs in that story: polished, portable,
and one file away from being shared.

**Ship the artifact, not the terminal scrollback.**
