# Changelog

## 0.1.1

- Add `vault` export: turn an Obsidian/VS Code/Markdown folder into a linked, self-contained HTML site.
- Add card + gallery demo screenshots.
- Publish to npm automatically on `v*` tags.

## 0.1.0

- Initial release: render Markdown + JSON into self-contained HTML documents, cards, dashboards, and galleries (5 themes, zero runtime dependencies, in-harness `artifact_render` tool).
